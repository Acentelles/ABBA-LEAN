# ABBA: verification status of the paper's results

Final status as of 2026-08-20. Lean project: `~/dev/abba-lean` (Lean 4.33, Mathlib v4.33, 25 modules, 15 commits `cb84b9f` through `e23abd9`, each a green `lake build` with zero `sorry`). Sage battery: `abba_checks.sage` and the distributional check (session scratchpad), all passing. Caveat: Lean verifies the statements as transcribed into `AbbaLean/*.lean`; the correspondence with the LaTeX statements is by inspection. "Numerical (exact)" means exhaustive or exact-rational computation, not sampling.

## Results this project worked on

| Result | Content | Lean | Numerical (Sage) | Remaining gap |
|---|---|---|---|---|
| Lemma `Lemma:structure` | $\Lambda_q \cong \prod_i M_2(\mathbb{F}_{q^{f_i}})$, $Z(\Lambda_q) = \mathcal{O}_{K_q}$ | **Fully machine-checked end to end.** Per factor: ring axioms, relations, center, split and inert cases, and the uniform theorem `uniformEquiv` (any commutative 2-dim $k$-algebra with a unit $c$, $c\theta(c) = \xi$, and a unit $\theta(w)-w$ gives $M_2(k)$; no Artin-Wedderburn); `Hilbert90.lean` (fixed points $= k$, Hilbert 90 via the range-dimension trick, norm surjectivity by counting; no etale dichotomy). Globally: quotient descent (`StructureQuot`), equivariant CRT `crtTwisted` (`StructureCRT`), assembly `structure_global`, discriminant discharge and `center_global` (`StructureDischarge`), per-factor $M_2(\mathcal{O}_K/\mathfrak{q}_i)$ (`StructureFactor`), number-field form `structure_numberField` (`StructureNumberField`), fixed points `fixed_global` (`StructureFixed`). **ABBA instantiation** (`AbbaInstance.lean`): `abba_structure` and `abba_fixed`, with residue finiteness from maximality, $\dim = 2$ from $[L:K]=2$, discriminant invertibility from $2 \notin \mathfrak{q}_i$ via $\delta \cdot (\zeta\sum_{j<m}\zeta^{2j})^2 = 4$, and $-1 \notin \mathfrak{q}_i$ from properness | Exact: unit counts, center, radical, $r \leq 5$, all split/inert combinations | None. Remaining *inputs* are the defining data of the setting, not proof obligations: $\sigma$ with $\sigma^2 = 1$, $\zeta$ with $\zeta\sigma(\zeta) = 1$, $\zeta^{2m} = -1$, $[L:K] = 2$, and the factorization of odd $q$ into distinct maximals. Optional polish: derive $(\sigma, \zeta)$ from `IsCMField`/cyclotomic instances; name the residue fields $\mathbb{F}_{q^{f_i}}$ |
| Lemma `Lemma:unitdensity` | $\rho = ((1-q^{-1})(1-q^{-2}))^n \geq 1 - 2n/q$ | Full, on the matrix-ring side (`UnitDensity.lean`: `card_GL2`, `card_units_pi`, `unit_density`, `unit_density_ge`) | Exact counts (small cases) and formula identity | **Transported** (`AbbaTransport.lean`: `abba_card_units`, the unit count of $\Lambda_Q$ itself is $\prod_i (q_i^2-1)(q_i^2-q_i)$); the poly-time decidability clause is out of scope (no complexity theory in Lean) |
| Prop `Propn:ICSIStoICSISx` | I-CSIS $\to$ I-CSIS$^\times$, halved samples | None (needs a probabilistic-game framework) | Exact: conditional uniformity on $\Lambda_3$ ($81^2$, $81^4$ enumeration, exactly uniform); abort prob $=$ binomial tail; Hoeffding chain in exact rationals ($m \leq 60$) | Lean formalization would be of the distributional core only |
| Prop `Propn:ICSIStoICSISxTight` | Additive-loss variant, $F_k$ bounds | None | Exact rationals: $F_k \leq \binom{m+k}{k+1}\delta^{k+1} \leq ((m+k)\delta)^{k+1}$, $2^{-(k+1)}$ case | Same as above |
| Prop `Propn:CSIStoICSIS` | CSIS $\to$ I-CSIS, $\beta_0 = \sqrt{\beta^2 + 2n}$ | Full for the deterministic core (`CsisToIcsis.lean`: `solution_map`, `solution_ne_zero`, `trace_one`) | Distributional step ($v := -a_{m+1}$) exact by construction | The PPT wrapper (not formalizable) |
| Norm floor (remark after `Propn:ICSIStoICSISx`) | $\lVert z \rVert \geq \sqrt{2n}$ for nonzero $z \in \Lambda$ | Full (`trace_sum_sq_ge`: AM-GM floor for totally real fields) | Sampled ($r \leq 5$, 300 samples each) | None |
| Rem `Rem:syndromegap` | $SD(U(\Lambda_q)^m, U(\Lambda_q^\times)^m) = 1 - \rho^m \leq 2nm/q$ | **Full** (`Polish.lean`: `sd_uniform_subset`, the half-$L^1$ identity $SD(U_A, U_B) = 1 - \lvert B\rvert/\lvert A\rvert$; `one_sub_pow_le`, the bound $1-\rho^m \leq m\varepsilon$) | Exact on $\Lambda_3$, $m \leq 3$ | None |
| Lemma `Lemma:centrality` | $\alpha$ central iff commutator-linear; $\alpha\,\mathcal{T}_0 \subseteq \mathcal{T}_0$ | **Full** (`Polish.lean`: `commutator_linear_iff`, `commutator_sum_linear`, `trace_smul_eq_zero`; counterexample in `Cap.lean`) | No | None |
| Lemma `Lemma:cap` | Central challenge sets have $\lvert \mathcal{C} \rvert \leq q^f$ | Full for the abstract core (`Cap.lean`; formalization added a needed `Nontrivial` hypothesis) | Exact max clique $= q^f$ at $g=2, f=1$; achievability at $f=2$ | **Transported** (`AbbaTransport.lean`: `abba_cap`, central pairwise-invertible-difference sets in $\Lambda_Q$ itself have at most $\lvert\mathcal{O}_K/\mathfrak{q}_{i_0}\rvert$ elements, via components of central elements being scalar matrices) |
| Lemma `Lemma:shortinvert` | Short coefficients $\Rightarrow$ invertible mod $q$ | **Full, including the $K^+$ form**: the three steps (`ShortInvert.lean`), the composed statement (`ShortInvertFull.lean`: `shortinvert_general`), and the instantiation at the totally real subfield (`ShortInvertTower.lean`: `embedding_le_of_isAlgebraic` descends embedding bounds along algebraic extensions via `IsAlgClosed.lift`; `shortinvert_tower`, `Hc_diff_tower` check the bound cyclotomically). The residue-degree hypothesis is rendered as $\mathrm{absNorm}\,M = q^F$ for maximals above $q$ | Exhaustive for $n = 16, 32$ (all in-hypothesis elements invertible, incl. the fully binding $n=32, q=67, f=8$ case) | None (modulo the $\mathrm{absNorm}$ vs. `inertiaDeg` phrasing, definitionally the same content) |
| Cor `Cor:Hc` | $H_c$ is a strong sampling set, $T \leq 2\varphi(n)c$ | **Invertibility clause full** (`Hc_diff`); **counting clause full** (`HcCard.lean`: challenge-family independence by the $z^{d-1}$-multiplication argument, coefficient-map injectivity, $\lvert H_c \rvert = (2c_b+1)^d$; power independence dischargeable from any `PowerBasis`) | Exhaustive difference-invertibility (as above); $5^{64} \approx 2^{148}$ checks | Expansion-factor clause ($T \leq 2\varphi(n)c$) references Neo's external definitions; out of formal scope |
| Def `def:admissible` / Rem `rem:admissibleprimes` | Admissibility arithmetic ($f$ of Goldilocks, $M_{61}$, $2^{64}-59$; 112/128 classes; $f$ quantization) | None | Exact, including degree-64 minpoly factorizations mod the actual 61-64 bit primes | Pure arithmetic; low value to formalize |

## Results not touched by this verification effort

| Result | Content | Status |
|---|---|---|
| Lemma `crt` | CRT decomposition of $\Lambda/\mathcal{I}\Lambda$ | **Fully machine-checked** in the quaternion case: `piEquiv` (`TwistedPi.lean`) plus the equivariant `crtTwisted` (`StructureCRT.lean`) with conjugation-stability of extended ideals (`StructureQuot.lean`); no longer imported |
| Lemmas `nonmaxtech`, `nonmaxiso`, `QuatComms`, `statdistcollisions`, `sampleD`, `modsmoothparam`, `lemma3`, `limitslp`, `hyperplaneprob`, `regev316`, `Thm3.1`, `modtailbd` | Imported preliminaries (cited) | Imported; not checked |
| Prop `nonmaxclearingmods`, Lemma `quatnorm` | Clearing ideals; coefficient-norm identity and conjugation invariance | Not checked (the floor derived from `quatnorm` is Lean-checked; the identity itself is not) |
| Thm `RedGIVPtoIncGIVP`, `SVP-ComSVP`, `approxSVPtoComIncGIVP` | Worst-case problem reductions | Not checked |
| `commprob0`, `charliel8`, `gensetT0`, `commprobunif`, `Dcollprobthm`, `UnifDistrCor` | Uniformity of the commitment image | Not checked |
| Thm `ThmComIncGIVPtoComSIS` (+ `DistrInputComSIS`, `hNotinHyperplane`, `hisShort`), `MainCor` | Main worst-case to average-case reduction | Not checked |
| Prop `Propn:nonmaxLambdaComSIS`, Thm `ThmCModInctoCModSIS`, Cor `Cor:CGIVPtoICSISx`, Prop `Propn:ComSIStoIComSIS`, Cor `Cor:worstcasetoIComSIS` | I-ComSIS grounding chain | Not checked (the composition logic of `Cor:CGIVPtoICSISx` rests on the checked/unchecked pieces above) |
| Appendix (`torsionquots`, `HNPmodDecomp`, `nonmaxpseudobases`, `app:cmodincthm`) | Pseudobasis and CModInc machinery | Not checked |
